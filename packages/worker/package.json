{
  "name": "@kanban/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc -b && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run --root ../..",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@kanban/protocol": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^4.85.0"
  }
}
